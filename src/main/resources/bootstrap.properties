# Spring Cloud Config - application name used for config server lookup
spring.application.name=fundoo-notes-service

# Config server URL (comment out if not running a config server locally)
# spring.config.import=optional:configserver:http://localhost:8888
